# Set these via the Notion CLI (never commit real values):
#   ntn workers env set GOOGLE_CLIENT_ID=... GOOGLE_CLIENT_SECRET=...
#
# For local development after `ntn workers env pull`:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
