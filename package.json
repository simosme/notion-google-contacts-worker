{
  "name": "notion-google-contacts-worker",
  "version": "1.0.0",
  "description": "Notion Worker that syncs Google Contacts to a managed Notion database every 3 hours",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.9.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.20.6",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@notionhq/workers": ">=0.0.0"
  }
}
